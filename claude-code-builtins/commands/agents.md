# /agents

| Field | Value |
|---|---|
| Description | (removed) Ask Claude to create/manage subagents, or edit .claude/agents/ |
| Aliases | — |
| Argument | — |
| Availability | — |
| Hidden | no |
| Enabled | yes |
| Immediate | no |
| Non-interactive | yes |

Extracted definition (raw snippet from the built-in command registry):

```js
),KOy,RXd;var kXd=S(()=>{KOy={type:"local",name:"agents",description:"(removed) Ask Claude to create/manage subagents, or edit .claude/agents/",supportsNonInteractive:!0,load:()=>Promise.resolve({call:zOy})},RXd=KOy});var IXd={};tt(IXd,{settingSourceToScope:()=>YOy,installSelectedPlugins:()=>JOy,getPluginEditableScopes:()=>ume,checkEnabledPlugins:()=>Yqs});async function Yqs(){let
```
